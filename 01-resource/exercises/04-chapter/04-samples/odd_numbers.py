# Use a função range() com três argumentos para gerar uma lista de números 
# ímpares de 1 a 20. Use um laço for para imprimir cada número.

impares = list(range(1, 21, 2))
for impar in impares:
    print(impar)